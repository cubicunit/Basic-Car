using UnityEngine;

namespace BasicCarParking {
    public enum GEARBOX {
        PARK = -2,
        REVERSE = -1,
        NEUTRAL = 0,
        DRIVE = 1
    }

    public enum GEARDIR {
        UP = 1,
        DOWN = -1
    }
}
